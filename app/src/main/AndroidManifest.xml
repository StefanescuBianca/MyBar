<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mybar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyBar">
        <activity android:name=".Activity_Smoothie"></activity>
        <activity android:name=".Activity_Shot" />
        <activity android:name=".Activity_MilkFloatShake" />
        <activity android:name=".Activity_CoffeeTea" />
        <activity android:name=".Activity_Cocktail" />
        <activity
            android:name=".MainActivity"

            android:label="@string/app_name"
            android:theme="@style/Theme.MyBar.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>